<?php
/**
 * Arquivo de credenciais - SEGURANÇA
 * 
 * INSTRUÇÕES:
 * 1. Copie este arquivo para credentials.php
 * 2. Preencha as credenciais abaixo
 * 3. Adicione credentials.php ao .gitignore para NUNCA commitar credenciais
 * 4. Em produção, prefira usar variáveis de ambiente (getenv())
 */
if (!defined('DF_CONFIG_LOADED')) {
    define('DF_CONFIG_LOADED', true);
}

// Use getenv('VAR') em produção para maior segurança
define('SMTP_HOST', getenv('SMTP_HOST') ?: 'smtpi.uni5.net');
define('SMTP_PORT', getenv('SMTP_PORT') ?: 587);
define('SMTP_USER', getenv('SMTP_USER') ?: 'seu_email@dominio.com');
define('SMTP_PASSWORD', getenv('SMTP_PASSWORD') ?: 'sua_senha');

// Chaves secretas do reCAPTCHA (uma por formulário)
define('RECAPTCHA_SECRET_ENVIA', getenv('RECAPTCHA_SECRET_ENVIA') ?: 'sua_chave_secreta');
define('RECAPTCHA_SECRET_ENVIA2', getenv('RECAPTCHA_SECRET_ENVIA2') ?: 'sua_chave_secreta');
define('RECAPTCHA_SECRET_APOIO', getenv('RECAPTCHA_SECRET_APOIO') ?: 'sua_chave_secreta');
